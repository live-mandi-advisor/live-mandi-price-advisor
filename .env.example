# Copy this file to .env and fill in real values. Never commit .env itself.

# data.gov.in Agmarknet API key (register free at https://www.data.gov.in)
AGMARKNET_API_KEY=your_key_here

# Database (SQLite locally by default; Supabase Postgres URL in production)
DATABASE_URL=sqlite:///./mandi.db
